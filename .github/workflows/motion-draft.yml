name: Retired first motion drafts
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'The old robot-only render is retired. Use the human animation review workflow and its current complete_actors.py source. No assets are generated or overwritten here.'
